---
- hosts: m910q
  tasks:
    - import_tasks: '../../tasks/infra/email/main.yml'

  handlers:
    - name: 'Restart Postfix'
      service:
        name: postfix
        state: restarted
      when: 'not("/.dockerenv"|exists)'
